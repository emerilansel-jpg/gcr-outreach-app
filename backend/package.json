{
  "name": "gcr-outreach-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply gcr-outreach-db",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "drizzle-orm": "^0.31.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.0",
    "drizzle-kit": "^0.22.0",
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0"
  }
}
